        // float MzLookup = Mz.Evaluate(Mathf.Abs(SA));
        // if (SA < 0){
        //     MzLookup *= -1;
        // }
        // if (Mathf.Abs(steer) != 1 && Mathf.Round(Mathf.Abs(SA)) > 0 && FrontTire){
        //     //Epic code for ffb :thumbsup:
        // }